<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Codevirus Hero</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800;900&display=swap" rel="stylesheet">

<style>
/* ================= RESET ================= */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Inter', sans-serif;
}

body {
  background: #020617;
  color: #fff;
  overflow-x: hidden;
  width: 100%;
  height: 100vh;
}

/* ================= HERO WRAPPER ================= */
.hero {
  position: relative;
  height: 100vh;
  width: 100%;
  overflow: hidden;
  display: flex;
  justify-content: flex-start;
  align-items: center;
  flex-direction: column;
  text-align: center;
  padding-top: 140px;
}

/* ================= STAR BACKGROUND ================= */
.space-bg {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(1px 1px at 10% 20%, #fff, transparent),
    radial-gradient(1px 1px at 30% 70%, #9fdcff, transparent),
    radial-gradient(1px 1px at 60% 40%, #fff, transparent),
    radial-gradient(1px 1px at 80% 90%, #7dd3fc, transparent),
    radial-gradient(circle at center, #020617 0%, #000 75%);
  background-size: 1200px 1200px;
  animation: spaceMove 120s linear infinite;
  z-index: 0;
}

@keyframes spaceMove {
  from { background-position: 0 0; }
  to { background-position: 3000px 3000px; }
}

/* ================= GRID FLOOR ================= */
.floor-grid {
  position: absolute;
  bottom: -55%;
  left: -60%; /* Adjusted to ensure it covers wide screens */
  width: 220%;
  height: 220%;
  background-image:
    linear-gradient(rgba(0,212,255,.28) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,212,255,.28) 1px, transparent 1px);
  background-size: 60px 60px;
  
  /* 3D Perspective match */
  transform: perspective(900px) rotateX(65deg);
  
  animation: gridScroll 14s linear infinite;
  
  /* Masking for the fade-out effect */
  mask-image: linear-gradient(to top, transparent 0%, black 40%);
  -webkit-mask-image: linear-gradient(to top, transparent 0%, black 40%);
  
  z-index: 1;
}

@keyframes gridScroll {
  from { background-position: 0 0; }
  to { background-position: 0 900px; }
}

/* ================= HORIZON GLOW ================= */
.horizon-glow {
  position: absolute;
  bottom: 20%;
  left: 0;
  width: 100%;
  height: 220px;
  background: radial-gradient(circle at center,
    rgba(0,212,255,.95),
    transparent 70%);
  filter: blur(12px);
  z-index: 2;
}

/* ================= HERO CONTENT ================= */
.hero-content {
  position: relative;
  z-index: 10;
  animation: float 6s ease-in-out infinite;
  padding: 0 20px;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-18px); }
}

/* Title Gradient match */
.hero-title {
  font-size: clamp(3rem, 9vw, 6.8rem);
  font-weight: 900;
  letter-spacing: 5px;
  background: linear-gradient(90deg, #22d3ee, #a78bfa, #fbcfe8);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  color: transparent; /* Fallback */
  
  text-shadow:
    0 0 40px rgba(0,212,255,.7),
    0 0 90px rgba(0,212,255,.5);
}

.hero-subtitle {
  margin-top: 16px;
  font-size: 1.25rem;
  color: #e5e7eb;
}

/* ================= BUTTONS ================= */
.hero-buttons {
  margin-top: 36px;
  display: flex;
  justify-content: center;
  gap: 20px;
  flex-wrap: wrap;
}

.btn-primary {
  padding: 14px 36px;
  border-radius: 30px;
  background: linear-gradient(135deg, #38bdf8, #3b82f6);
  color: #000;
  font-weight: 600;
  text-decoration: none;
  box-shadow: 0 20px 60px rgba(56, 189, 248, .6);
  transition: .3s;
  display: inline-block;
}

.btn-outline {
  padding: 14px 36px;
  border-radius: 30px;
  border: 1px solid #38bdf8;
  color: #38bdf8;
  text-decoration: none;
  font-weight: 600;
  transition: .3s;
  display: inline-block;
  background: transparent;
}

.btn-primary:hover,
.btn-outline:hover {
  transform: translateY(-5px);
  box-shadow: 0 30px 90px rgba(56, 189, 248, .8);
}

/* ================= STATS ================= */
.stats {
  position: absolute;
  bottom: 42px;
  display: flex;
  gap: 80px;
  color: #cbd5f5;
  font-size: .95rem;
  z-index: 10;
  width: 100%;
  justify-content: center;
}

.stats span {
  opacity: .9;
}

/* ================= RESPONSIVE ================= */
@media(max-width: 768px) {
  .stats {
    flex-direction: column;
    gap: 18px;
    align-items: center;
  }
}
</style>
</head>

<body>

<section class="hero">
  <div class="space-bg"></div>
  <div class="floor-grid"></div>
  <div class="horizon-glow"></div>

  <div class="hero-content">
    <h1 class="hero-title">CODEVIRUS</h1>
    <p class="hero-subtitle">Learn. Build. Secure. Your Future in Tech 🚀</p>

    <div class="hero-buttons">
      <a href="#" class="btn-primary">Explore Courses</a>
      <a href="#" class="btn-outline">Join Free</a>
    </div>
  </div>

  <div class="stats">
    <span>🎓 10,000+ Students</span>
    <span>💻 Industry-Ready Courses</span>
    <span>🔐 Cyber & Dev Focused</span>
  </div>
</section>

</body>
</html>