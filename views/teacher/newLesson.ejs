<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Lesson</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f6f8;
            padding: 40px;
        }

        h1 {
            margin-bottom: 20px;
        }

        form {
            max-width: 400px;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        input, button {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
        }

        button {
            background: #2563eb;
            color: white;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background: #1d4ed8;
        }
    </style>
</head>
<body>

<h1>Add Lesson – <%= course.title %></h1>

<form method="POST" action="/teacher/course/<%= course._id %>/lesson/new">

    <!-- Lesson Title -->
    <input 
        type="text" 
        name="title" 
        placeholder="Lesson Title" 
        required
    />

    <!-- Video URL -->
    <input 
        type="text" 
        name="videoUrl" 
        placeholder="Video URL (YouTube / MP4 / Cloudinary)" 
        required
    />

    <!-- Lesson Order (IMPORTANT) -->
    <input 
        type="number" 
        name="order" 
        placeholder="Lesson Order (1, 2, 3...)" 
        required
        min="1"
    />

    <button type="submit">Add Lesson</button>

</form>

</body>
</html>
